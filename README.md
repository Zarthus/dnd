# dnd

A website

## Install

```bash
npm install
npm build
```

## Running

A webserver pointing to the `public/` folder should do the trick. For dev purposes, browsersync will do:

```
npm start
```
